<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="/style2.css">
  
  <title>inventory management App</title>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/inventory">Home</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/addProduct">Add new product</a>
              
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Account
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/editaccount/<%= globaluserid%>">Manage Account</a>
                    <a class="dropdown-item" href="/deleteaccount/<%= globaluserid%>"onclick="return confirm('Are you sure you want to delete the account')">Delete Account</a>
                  <a class="dropdown-item" href="/">Log Out</a>
                </ul>
              </li>
            </ul>
          </div>
        </div>
       
      </nav>
    <div class="container">
    <br>
    <div class="text-center"><h2>Products from inventory App Database</h2></div>
    <br>

    <% if (inventory && inventory.length > 0) { %>
      <table class="table table-hover small text-center">
        <thead>
            <tr>
              <th width="100">Product Name</th>
              <th width="100">Product Image</th>
              <th width="50">Quantity</th>
              <th width="50">Price</th>
              <th width="50">Edit</th>
              <th width="50">Delete</th>
            </tr>
        </thead>
        <tbody>
          <% for(let i = 0; i < inventory.length; i++) { %>
            <tr>
              <td><a href="/products/<%= inventory[i].productid %>" ><%=inventory[i].productname %></a></td>
              <td><img src= "/<%= inventory[i].image %>" width="20%"></td>
              <td><%= inventory[i].quantity %></td>
              <td>$<%= inventory[i].price %></td>
              <td><a href="/editProduct/<%= inventory[i].productid %>"class="btn btn-primary">Edit</a></a></td>
              <td><a href="/deleteProduct/<%= inventory[i].productid %>" onclick="return confirm('Are you sure you want to delete the product')"class="btn btn-primary">Delete</a></td>
            </tr>
          <% } %>
        </tbody>
      </table>
    <% } else { %>
      <div class="text-center">
        <p>No products found in the inventory.</p>
      </div>
    <% } %>
  </div>
</body>
</html>
